import { UserSession } from "@esri/arcgis-rest-auth";
/**
 * Given a starting name, return a service name that is unqieu within
 * the current users organization
 *
 * @export
 * @param {string} name
 * @param {UserSession} session
 * @param {number} step
 * @return {*}  {Promise<string>}
 */
export declare function getUniqueServiceName(name: string, type: string, session: UserSession, step: number): Promise<string>;

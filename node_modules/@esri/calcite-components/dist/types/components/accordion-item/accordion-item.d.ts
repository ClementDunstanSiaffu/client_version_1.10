import { EventEmitter, VNode } from "../../stencil-public-runtime";
/**
 * @slot - A slot for adding custom content, including nested `calcite-accordion-item`s.
 */
export declare class AccordionItem {
  el: HTMLCalciteAccordionItemElement;
  /**
   * When true, the component is active.
   *
   * @deprecated use expanded instead
   */
  active: boolean;
  /** When true, item is expanded */
  expanded: boolean;
  /**
   * Specifies a title for the component.
   *
   * @deprecated Use `heading` instead.
   */
  itemTitle?: string;
  /**
   * Specifies a subtitle for the component.
   *
   * @deprecated Use `description` instead.
   */
  itemSubtitle?: string;
  /** Specifies heading text for the component. */
  heading?: string;
  /** Specifies a description for the component. */
  description: string;
  /** Specifies an icon to display - accepts Calcite UI icon names. */
  icon?: string;
  /**
   * @internal
   */
  calciteInternalAccordionItemKeyEvent: EventEmitter;
  /**
   * @internal
   */
  calciteInternalAccordionItemSelect: EventEmitter;
  /**
   * @internal
   */
  calciteInternalAccordionItemClose: EventEmitter;
  /**
   * @internal
   */
  calciteInternalAccordionItemRegister: EventEmitter;
  connectedCallback(): void;
  componentDidLoad(): void;
  render(): VNode;
  keyDownHandler(e: KeyboardEvent): void;
  updateActiveItemOnChange(event: CustomEvent): void;
  private guid;
  /** the containing accordion element */
  private parent;
  /** position within parent */
  private itemPosition;
  /** the latest requested item */
  private requestedAccordionItem;
  /** what selection mode is the parent accordion in */
  private selectionMode;
  /** what icon position does the parent accordion specify */
  private iconPosition;
  /** what icon type does the parent accordion specify */
  private iconType;
  /** handle clicks on item header */
  private itemHeaderClickHandler;
  private determineActiveItem;
  private emitRequestedItem;
  private getItemPosition;
}
